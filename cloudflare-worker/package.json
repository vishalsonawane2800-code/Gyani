{
  "name": "ipogyani-cron-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that triggers the IPOGyani Next.js cron endpoints on a schedule.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "types": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240924.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.80.0"
  }
}
